#!/bin/bash
# Shebang line: Indicates the path to the shell interpreter (in this case, bash)

# Script to concatenate the contents of two text files and display the result

# Using cat command to concatenate the contents of two text files and display the result
cat file1.txt file2.txt
# Cat command: Concatenates the contents of the specified files ("file1.txt" and "file2.txt") and displays the result